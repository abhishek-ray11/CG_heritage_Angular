import { TruncateText } from './truncate-text';

describe('TruncateText', () => {
  it('should create an instance', () => {
    const directive = new TruncateText();
    expect(directive).toBeTruthy();
  });
});
